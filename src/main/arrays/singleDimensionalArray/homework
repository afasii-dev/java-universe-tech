
1. Имплементировать метод, который будет суммировать длину (lenght()) всех элементов строкового массива. Вывести сумму в консоль.
2. Имплементировать метод, который будет конкатенировать (concat()) все элементы  строкового массива. Вывести результат в консоль.
3. Имплементировать метод, который будет обрезать пробелы (trim()) элементов строкового массива. Вывести результаты в консоль.
4. Имплементировать метод, который будет проверять - заканчивается ли элемент строкового массива на букву "а" (endsWith()). Если заканчивается, тогда выводим в консоль.
5. Имплементировать метод, который будет перезаписывать каждое второе значение элемента массива в верхний регистр (toUpperCase()). Вывести результат в консоль.
6. Имплементировать метод, который будет перезаписывать каждое третье значение элемента массива в нижний регистр. Вывести результат в консоль.
7. Имплементировать метод, который будет проверять на идентичность текущий элемент и определенную строку (equals()). Если идентичен - выводим в консоль.